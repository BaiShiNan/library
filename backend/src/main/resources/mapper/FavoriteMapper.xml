<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.backend.mapper.FavoriteMapper">
    <resultMap id="BaseResultMap" type="com.library.backend.entity.Favorite">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="book_id" property="bookId" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, book_id, created_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM favorites
        WHERE id = #{id}
    </select>

    <select id="selectByUserAndBook" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM favorites
        WHERE user_id = #{userId} AND book_id = #{bookId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM favorites
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorites (user_id, book_id, created_at)
        VALUES (#{userId}, #{bookId}, #{createdAt})
    </insert>

    <delete id="deleteById">
        DELETE FROM favorites WHERE id = #{id}
    </delete>

    <delete id="deleteByBookId">
        DELETE FROM favorites WHERE book_id = #{bookId}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM favorites WHERE user_id = #{userId}
    </delete>
</mapper>
